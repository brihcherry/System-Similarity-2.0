import { SystemSimilarityPageNew } from "./SystemSimilarityPage-New";

/**
 * Standalone System Similarity Heatmap page — uses the new duplicated copy
 * from features/systemSimilarityNew so it can evolve independently.
 *
 * @component
 */
export const SystemSimilarityHeatmapPage = () => {
	return <SystemSimilarityPageNew />;
};
